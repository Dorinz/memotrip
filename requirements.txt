# build_trip.py        - stdlib only
# select_photos.py      - Pillow always; google-genai only for the --ai path
# fetch_photos.py       - the google-auth trio

Pillow>=10.4

# fetch_photos.py (Google Photos Picker API)
google-auth>=2.30
google-auth-oauthlib>=1.2
requests>=2.32

# select_photos.py, --ai photo selection (optional; --no-ai needs none of this)
google-genai>=0.8

# parse_docs.py (logistics docs -> draft spec)
pypdf>=4.0
python-docx>=1.1

# webapp/ (Phase 4 — local MVP)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
tzdata>=2024.1  # IANA tz database for zoneinfo - stdlib on Linux, but Windows/some minimal
                # containers ship none, so this is required for ZoneInfo("Asia/Jerusalem")

# webapp/ accounts + guest mode
bcrypt>=4.1
itsdangerous>=2.2

# db.py (Postgres backend, prod - Cloud SQL; unused when DATABASE_URL is unset)
psycopg2-binary>=2.9

# tasks.py (Cloud Tasks backend, prod; unused when CLOUD_TASKS_QUEUE is unset)
google-cloud-tasks>=2.16
